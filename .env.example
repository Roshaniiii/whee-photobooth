# Filter API base URL (no trailing slash).
# Local backend: http://localhost:8000
# Production:   https://api.yourdomain.com
# Leave empty to ship frontend-only (CSS filters; backend filters hidden).
VITE_API_URL=http://localhost:8000
